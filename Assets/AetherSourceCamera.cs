using UnityEngine;
using System.Collections;

public class AetherSourceCamera : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GetComponent<Camera>().enabled = false;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
